import {
  inicializarFormulario,
  getComboBoxControllers,
} from "./controllers/formControllers.js";

document.addEventListener("DOMContentLoaded", () => {
  inicializarFormulario();
  getComboBoxControllers();
});
